#include <stdio.h>
#include "constants.h"

int main(void) {
    /*here will be a macro command that get the file and returns a new file with macro spread out*/
    /*here we go through the file to check for syntax errors*/
    return 0;
}
